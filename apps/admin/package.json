{
  "name": "@aimers/admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AIMERS OS Company Administration",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  }
}
