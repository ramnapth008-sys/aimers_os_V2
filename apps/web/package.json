{
  "name": "@aimers/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AIMERS OS Student Application",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  }
}
